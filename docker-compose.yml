version: "3.9"

services:
  mysql:
    image: mysql:8
    container_name: mysql-db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: sat
      MYSQL_DATABASE: cloud
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql
    networks:
      - satyam 

  nodeapp:
    build: .
    container_name:  backend-net
    restart: always
    ports:
      - "3000:3000"
    depends_on:
      - mysql
    env_file:
      - .env
    networks:
      - satyam 

networks:
  app-network:

volumes:
  mysql-data:
